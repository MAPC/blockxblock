import { expect } from 'chai';

import { describe, it } from 'mocha';
import { consructRelatedInvestmentsMarkup } from 'cityxcity/helpers/consruct-related-investments-markup';

describe('Unit | Helper | consruct related investments markup', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = consructRelatedInvestmentsMarkup(42);
    expect(result).to.be.ok;
  });
});


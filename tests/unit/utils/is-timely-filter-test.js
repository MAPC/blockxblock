import isTimelyFilter from 'blockxblock/utils/is-timely-filter';
import { module, test } from 'qunit';

module('Unit | Utility | is timely filter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isTimelyFilter();
  assert.ok(result);
});

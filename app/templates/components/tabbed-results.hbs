<div class="tabbed-results">
  <div class="ui pointing secondary tabular menu">
    <a class="item active" data-tab="features">Places and Investments</a>
    <a class="item" data-tab="parcels">Available Spaces</a>
  </div>
  <div class="ui tab active" data-tab="features">
    <div class="ui two column stackable grid cards">
      {{#each (sort-by "name" (shuffle (union visibleFeatures visibleInvestments))) as |card|}}
        <div class="column">
          {{#tdi-card modelContext=card 
                      image=(or card.splash card.iconUrl)
                      height=150}}
            {{#if card.feature_name}}
              {{card.feature_name}}
            {{else}}
              ${{number-format card.amount_estimated decimals=0}} - {{card.project}}
            {{/if}}
          {{/tdi-card}}
        </div>
      {{else}}
        <h3 style="margin: 0 auto; color: rgba(0,0,0,0.44);">
          No Results
        </h3>
      {{/each}}
    </div>
  </div>
  <div class="ui tab" data-tab="parcels">
    <div class="ui two column grid cards">
      {{#each (union (filter-by 'latest_is_for_sale' true visibleParcels) (filter-by 'latest_is_for_lease' true visibleParcels)) as |parcel|}}
        <div class="column">
          {{#tdi-card route='cities.city.parcels' 
                      modelContext=parcel 
                      image=parcel.splash
                      height=100}}
            {{parcel.address}}
          {{/tdi-card}}
        </div>
      {{else}}
        <h3 style="margin: 0 auto; color: rgba(0,0,0,0.44);">
          No Results
        </h3>
      {{/each}}
    </div>
  </div>
</div>

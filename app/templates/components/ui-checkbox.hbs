<input type={{type}}
       name={{name}}
       tabindex={{tabindex}}
       checked={{unbound checked}}
       disabled={{unbound disabled}} />
{{#if hasBlock}}
  <label>{{yield (action "execute")}}</label>
{{else}}
  <label>{{label}}</label>
  {{yield (action "execute")}}
{{/if}}

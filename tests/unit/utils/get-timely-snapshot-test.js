import getTimelySnapshot from 'blockxblock/utils/get-timely-snapshot';
import { module, test } from 'qunit';

module('Unit | Utility | get timely snapshot');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getTimelySnapshot();
  assert.ok(result);
});

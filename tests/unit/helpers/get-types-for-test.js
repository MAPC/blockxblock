import { expect } from 'chai';

import { describe, it } from 'mocha';
import { getTypesFor } from 'cityxcity/helpers/get-types-for';

describe('Unit | Helper | get types for', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = getTypesFor(42);
    expect(result).to.be.ok;
  });
});

